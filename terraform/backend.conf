# Copy this file to backend.conf.local and fill in the values
# Then run: terraform init -backend-config=backend.conf.local

resource_group_name  = "tf-state-rg"
storage_account_name = "tfstatestorageaccount" # Replace with your actual storage account name
container_name       = "tfstate"               # Replace with your actual container name
key                  = "terraform.tfstate" 